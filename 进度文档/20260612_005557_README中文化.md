# README 中文化进度

## 本次目标

将项目根目录 `README.md` 从英文说明改为中文说明，并同步补齐当前 WebQGIS 的实际架构、启动方式、API 概览、V1 边界和开发提示。

## 做了哪些

- 将 README 主体从英文改为中文。
- 补充 WebQGIS 的项目定位：面向 PostGIS 矢量数据的类 QGIS WebGIS MVP。
- 明确展示链路和编辑链路分离：MVT 用于展示，编辑时回源读取原始 PostGIS 几何并写回数据库。
- 更新当前能力说明，包含：
  - 数据源连接测试与注册。
  - 空间表扫描。
  - 图层注册。
  - MVT 瓦片接口。
  - 原始要素读取。
  - 要素新增、更新、删除。
  - Vue 3 + OpenLayers 工作台能力。
- 补充当前技术架构：
  - `apps/api`：NestJS + Fastify Adapter + pg。
  - `apps/web`：Vue 3 + Vite + OpenLayers + UnoCSS + VueUse。
- 补充后端 NestJS 模块说明。
- 补充前端工作台组件和 composable 说明。
- 保留并中文化快速开始、常用命令和 API 概览。
- 补充 V1 边界和开发提示。

## 没做哪些

- 未修改任何业务代码。
- 未新增或调整测试。
- 未改动 package 配置、前端页面、后端接口或构建脚本。
- 未对 README 添加截图或架构图。

## 哪里有 bug 或风险

- README 中描述的 PostGIS 端到端能力仍需要真实示例库进一步验证。
- README 暂未包含数据库样例 SQL，后续新开发者仍需要自行准备 PostGIS 表。
- README 暂未包含部署说明、环境变量示例和生产安全配置说明。

## 建议后续做什么

- 增加 `docs/` 或 README 子章节，提供示例 PostGIS 建表 SQL。
- 增加 `.env.example`，说明 `API_HOST`、`API_PORT` 等运行配置。
- 增加开发截图或简短架构图，帮助快速理解前后端模块边界。
- 在真实 PostGIS 示例库跑通后，把完整演示步骤写入 README。

## 改了哪些文件

- `README.md`
- `进度文档/20260612_005557_README中文化.md`

## Git 提交信息

- 当前分支：`main`
- 计划提交信息：`将 README 改为中文说明`
- 提交状态：待执行 `git status`、`git add`、`git commit`、`git push origin main` 后更新。
